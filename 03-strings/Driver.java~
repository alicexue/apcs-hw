public class Driver {
    public static void main(String[] args){
	String s="alice xue";
	String first=Str.substring(0, Str.indexOf(' '));
	String last=Str.substring(Str.indexOf(' ')+1);
	return first;
	return last;
    }
}